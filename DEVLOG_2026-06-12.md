# Collie Development Log — 2026-06-12

Theme of the day: **make the engine read an Excel model the way Claude does —
orient first, read whole tabs, comprehend — and begin the pivot from a
checklist-and-human-gate to a comprehension-first read whose trust comes from
cross-checks, not a human.** This is also the first log in **collie_v2**, a full
fork of collie_v1 created at the end of the day to isolate that pivot from the
production engine.

Reference target all day: a generic-Claude read of the St Regis model
(`Outcome.rtf`) — Overview · Key Deal Stats · Debt · Returns · Model Structure,
produced from ~3 sheet reads, no validation, no human confirm. That output is
the bar.

---

# Part 0 — Repo plumbing

- The new Streamlit app pointed at a fresh GitHub repo (`collie_v1`) that had
  been populated via the web uploader, which silently drops folders — so
  `scenarios/`, `knowledge/`, `.streamlit/` never made it up and the app
  crashed on `import scenarios._llm`. Fixed by force-pushing the complete local
  tree; verified a fresh clone boots HTTP 200.
- Moved `origin` from `fastastic_beast` to `collie_v1`. `git push` now deploys
  the repo Streamlit watches.

---

# Part I — Workbook Orientation (new first stage)  ·  331b2b9

## Trigger
The user kept asking why "identify the workbook" never changed extraction. The
answer: sheet identification existed (`sheet_classifier`, name tiers) but it
only ever fed a **candidate ranker** — it decided which of two NOI cells wins,
never which sheet to *read*. Every path led to "find metric X"; none led to
"understand sheet Y."

## Build
`workbook_orientation.py` — deterministic, read-only, no GPT. Classifies every
sheet's role (summary / inputs / returns / model / support) from CONTENT and
STRUCTURE:
- keyword concentration (acquisition / returns / debt / identity vocab)
- hardcode ratio (inputs are typed-in, not formulas)
- internal-formula density vs cross-sheet pulls (a summary PULLS values; an
  inputs sheet is REFERENCED BY others)
- time-series shape (model tabs carry wide dated roll-ups)

Sheet names are only a weak score bonus. Output is a workbook map; tier 99
name-skips (sensitivity/comps/backup) stay skipped regardless of content
(sensitivity grids LOOK like returns sheets — that's the trap).

Feeds the existing machinery via `orientation_tier_map()` → the `{sheet: tier}`
dict `extract_aam` / `scan_workbook_for_candidates` already consume. Visible
🧭 panel in the left column (roles, confidence, signals) so the user can verify
the engine's read before extraction.

## Validation (4 real models, deterministic)
425 Colorado + Retail byte-identical to baseline; BAC + St Regis strictly
improved. St Regis Interest Rate Spread 5.76% (stray One-Pager cell) → **3.20%
(Debt Information)** matching the chat read's "LIBOR + 320bps"; rate cap 2.50%
and LTV 69.73% recovered; a garbage exit cap (2,238,921×) eliminated. Key-sheet
role checks 9/10.

---

# Part II — Whole-sheet GPT reads  ·  331b2b9

## Reframe
Generic Claude read the Executive Summary as a *document*; our focused gap-fill
read a flattened list of 450 label/value pairs. Pairs destroy table structure
and discard everything off-catalog (the basis build-up, the IRR-tracker change
log, the red/green threshold).

## Change
The AAM focused GPT pass now sends the **whole text of the analyst's stack**
(`select_read_sheets` quotas + `render_sheets_text` with A1 refs and `*`
hardcode marks) instead of the pair list. Prompt rewritten for sheet-reading
(units-header warning, portfolio-slice warning). **Grounding, schema validation,
and the gap-status discipline are unchanged** — comprehension got better; the
trust machinery didn't move. Pair list kept as fallback.

Mocked-GPT test: true claim accepted, fabricated value rejected, off-whitelist
citation rejected → NOI re-derived from pricing.

---

# Part III — Snapshot reads the analyst's pages  ·  64b48fa

`analyst_reading_stack()` (orient → select → render, name-tier fallback) added.
The Snapshot prompt gained an `AUTHORITATIVE SHEET CONTENT` section — the full
one-pager/inputs pages — for the deal STORY, with an explicit rule: **bounded
(human-verified) metrics GOVERN on any numeric disagreement.** The comprehension
upgrade can't override the gate.

---

# Part IV — Agent-driven deep dives + model upgrades  ·  1523d1a

- A deep-dive chip is now a TASK for the tool-equipped agent: read the relevant
  sheets (`read_sheet`/`search_file`, guided by the orientation map), cross-check
  the verified SSOT (human values govern), then write the section — reproducing
  "dig into returns → ran 3 commands". Static single-shot dives kept as fallback.
- Model upgrades on the comprehension surfaces: agent loop, AAM gap-fill, and
  the final report → **gpt-4o**. Bulk/cost-sensitive calls (classifier, insight
  pass, section reader, pool resolver) stay on **gpt-4o-mini**.
- `scenarios/_llm` client consolidated into a lazy, key-aware `get_client()`
  (db8daec) so a key set after start is picked up.

---

# Part V — Ingest latency  ·  f603eb7

Profiling: St Regis upload→gate paid ~28s; the bigger waste was post-confirm
re-extraction of metrics the human had JUST verified.

1. **Gate-record adoption** — `_run_bounded_extraction` adopts bounded metrics
   that already resolved at the gate (verified/candidate_pool/derived/inferred);
   only the remainder runs section reads / GPT fallback / pool resolution. On BAC
   17/34 skip GPT work. Prefilled runs aren't disk-cached (they embed session
   state).
2. **Orientation disk cache** (`cache/orientation/`, sha256+version): St Regis
   re-orientation 14.5s → 0.01s.
3. **Grid scan** — with a tier map, `scan_workbook_for_candidates` materializes
   only whitelisted sheets via one read-only sweep (`_GridSheet`) instead of a
   full openpyxl load. Byte-identical candidates on all 4 models; St Regis scan
   10.7s → 6.2s. `extract_raw_labeled_pairs` moved to the same loader (11.5s →
   4.6s).

St Regis upload→gate ~28s → 3.8s warm.

---

# Part VI — Retire the legacy Pass-1 scan (UW)  ·  3a7d671

The underwriting ingest ran a SECOND full-catalog scan (`extract_from_file`,
~14.5s on St Regis) purely to populate `layer["metrics"]` — redundant with the
bounded pipeline. Retired for the UW layer: `layer["metrics"]` is now
synthesized from the reconciled bounded records (same shape, canonical names),
so every consumer keeps working. **Other layers (actuals_*, business_plan) keep
the legacy scan** — perf_vs_plan reads layer metrics the bounded checklist
doesn't cover, and they have no gate to synthesize from.

---

# Part VII — The comprehension-first pivot + `model_brief.py` (step 1)  ·  dde6147

## User reframe
> "This is the result I want from any excel model … without going through the
> validation process (validation works, so no human confirm). Quick, no wrong
> data, focused. Work like Claude reads Excel."

The gap to `Outcome.rtf` is NOT a validation gap. The checklist-and-gate **is**
the architecture; the Outcome is a document read. Half its richness (Fund,
Strategy, WDP, Peak Equity, Yield-on-Cost, Model Structure) is outside our 22
fields. The fix is to make comprehension the product and turn validation from a
human GATE into a silent per-number CONFIDENCE SCORE.

## Target pipeline
```text
Upload → Orient(cached) → Read key tabs whole → Comprehend(facts+brief, 1 call)
       → Trust engine → Render: show / flag / omit → Deep dives on demand
```
Trust engine = 5 signals replacing the human: **grounded** (cell is real),
**authoritative** (oriented summary/inputs/returns tab), **reconciles** (Price ×
Cap ≈ NOI, Debt + Equity ≈ Basis, …), **corroborated** (agrees across summary
tabs), **challenged** (a second read told to find a *better/different* value —
the model argues with itself). Composed figures (per-key, totals) are recomputed
deterministically. HIGH → show plain; MED → inline flag; LOW/conflict → omit.

## Built today — step 1 only (comprehension, NO trust yet)
`model_brief.py`: orient → `analyst_reading_stack` → ONE gpt-4o call returning
`{identity, facts[cell-cited], brief{overview,key_stats,debt,returns,
model_structure}}`. Disk-cached by file hash + version + model. Rendered in the
Outcome column ABOVE the audit gate, read-only — the gate is untouched. No-key
guard keeps the gate working without the LLM.

Deliberately comprehension-only so we can judge raw read quality against the
reference Outcome **before** building the trust engine that earns removing the
gate.

Mocked-GPT validation: brief assembles into the Outcome shape; reads the right
tabs (Executive Summary / One-Pager / General Information on St Regis — the chat
read's list); cache round-trips; no-key guard clean; app boots.

## Why a new repo
The pivot moves away from the gated philosophy the production engine is built
on. **collie_v2** is a full fork (complete history) so the rebuild is isolated;
collie_v1 stays the working, gated engine. Pushed public to
github.com/hello-jhb/collie_v2.

---

# Current State (end of 2026-06-12, collie_v2)

**Built + validated headless:** Workbook Orientation (content-based sheet map,
visible panel) · whole-sheet GPT reads at the gate · whole-sheet Snapshot
context · agent-driven deep dives · gpt-4o on comprehension surfaces · ingest
latency cuts (gate adoption, orientation cache, grid scan) · legacy Pass-1
retirement (UW) · `model_brief.py` comprehension read (step 1) rendered above
the gate.

**Needs the live key — QC run:** the actual quality of the Model Brief vs
`Outcome.rtf`, and the deployed gap-fill / Snapshot / deep-dive quality on gpt-4o.

**Known trade-off:** orientation can mislabel template tabs on huge models
(St Regis DW-warehouse tabs roled 'summary' — harmless to extraction, visible in
the panel). Agentic dives now cost ~2–6 LLM calls each on gpt-4o.

---

# Deferred / next (the trust engine — what earns removing the human)

1. **Trust engine checks 1–4** — grounding (have `_grounded_value`), authority
   (have orientation), reconciliation (have `run_identity_checks` + NOI derive),
   corroboration across summary tabs (new, cheap). Per-fact verdict.
2. **Challenge read (check 5)** — second cheap pass told to find a better/
   different value; conflict → flag. Composed-figure recompute.
3. **Tiered render + optional correction; demote the gate** to exception-only.
4. **Drop the subsumed UW stages** — section reader, fallback, pool resolution,
   comprehension review, Pass 2 (the whole-sheet read subsumes them). The speed
   win.
5. **Go/no-go checkpoint:** after checks 1–4, measure HIGH vs flagged on a real
   model. If most facts land HIGH, removing the gate is data-backed; if not,
   tune before flipping the UI.

Also still open from prior logs: multi-open consolidation (the 18MB file is
opened several times per ingest); prompt/knowledge-injection alignment (the old
per-metric prompts don't match the whole-sheet philosophy; dives/report/Pass 2
don't see active knowledge patterns); the `model_navigator.py` rename if
filenames should match the plan diagram.
